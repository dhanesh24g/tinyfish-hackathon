# Runtime frontend config. These are read server-side by /api/config and can be
# injected at runtime via Docker, AWS ECS, Google Cloud Run, Azure, or Vercel
# environment variables without rebuilding the image.
API_BASE_URL=http://localhost:8000
SUPABASE_URL=
SUPABASE_ANON_KEY=
